const ten = 10;
const two = 2;

const config = {
	components: ['Logo', 'Pendulum', 'FlexBox', 'TreasureBox'],
	flexColor: ['red', 'green', 'blue'],
	number: Math.floor((Math.random() * ten) + two),
};

export default config;
